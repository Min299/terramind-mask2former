torch>=2.1
torchvision
pytorch-lightning
scipy
tqdm
numpy
pyyaml
terratorch

# detectron2 has no official PyPI wheel and must match your installed
# torch/CUDA version. Install it separately, e.g.:
#   pip install 'git+https://github.com/facebookresearch/detectron2.git'
# See https://detectron2.readthedocs.io/en/latest/tutorials/install.html
